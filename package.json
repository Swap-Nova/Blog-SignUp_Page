{
  "name": "blog_signup-page",
  "version": "1.0.0",
  "description": "A backend application that is built upon private APIs that stores the name and e-mail of the client to subscribe them to the newsletter.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "request": "^2.88.2"
  }
}
